 public int MinSubArrayLen(int target, int[] nums) {
    //滑块窗口
    //1.窗口结束移动：i,j=0,j++；if(sum>=target) break;
    //2.窗口起始移动：i++; if(sum<target) break;
    //
    //2.终止条件：j=length-1,当i=?时，sum<target
    //...
    //3.直到target=0；


}